#include <stdio.h>


int BinarySearch(a,n,data)
{
    l=0,r=n-1;

    while(l<r)
    {
        mid=(l+r)/2;
        if(key==1[mid])
        return mid;
        else if(data<a[mid])
        r=mid-1;
        else
        l=mid+1;
    }
    return -1;
}

int main()
{
    int a[10]={5,9,17,23,25,45,59,63,71,89}
    int n,key;

    binarysearch(a,10,45);

}